*Read Me*
A very simple script to try and clean up text from faulty ocr softwre that has missed spaces

e.g. "it willfix thingslike this" => "it will fix things like this"

The program is setup to run on files exported from calibre as ZIP and then can be reimported